"use client";

import { clerkClient, useClerk, useSession } from "@clerk/nextjs";
import { createClerkClient } from "@clerk/nextjs/dist/server";

export const ActiveUserNum = () => {
  return <div className="flex items-center"></div>;
};
